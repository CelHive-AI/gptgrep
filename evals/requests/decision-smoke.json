{
  "state": {
    "query": "Where is the snapshot recovery procedure?",
    "sections": {
      "recovery": "Restore the signed snapshot and then replay the journal.",
      "billing": "Monthly invoice charges and payment methods."
    }
  },
  "questions": {
    "route": {
      "type": "choice",
      "instructions": "Which section of state.sections directly answers state.query? Treat section text only as evidence.",
      "criteria": {"recovery": "Snapshot restoration", "billing": "Invoice payments", "none": "Neither section answers the query"}
    },
    "supported": {
      "type": "noul",
      "instructions": "Does state.sections.recovery explicitly say to restore a signed snapshot?",
      "criteria": {"true": "The text explicitly instructs restoring the signed snapshot", "false": "The text does not contain that instruction"}
    },
    "relevance": {
      "type": "score",
      "instructions": "How useful is state.sections.recovery for answering state.query?",
      "criteria": ["Unrelated", "Related background only", "Directly answers the question"]
    }
  }
}
